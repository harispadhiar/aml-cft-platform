# risk-scoring-engine

Status: BUILT (Phase 0). Deterministic, rules-only. No ML yet — see Section 5/
Phase 2 of the architecture doc for when ensemble ML scoring is added
alongside (not replacing) this.

Drop models.py / weights.py / engine.py / demo.py into `risk_scoring/`.
Run: `python3 -m risk_scoring.demo` from this directory.
